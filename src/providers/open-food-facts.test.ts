import {afterEach,describe,expect,it,vi} from "vitest";import {OpenFoodFactsProvider} from "./open-food-facts";
describe("OFF adapter",()=>{afterEach(()=>vi.unstubAllGlobals());it("normalizes nullable nutrients and provenance",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue(new Response(JSON.stringify({status:1,product:{code:"12345678",product_name:"Test",nutriments:{"energy-kcal_100g":42}}}),{status:200})));const item=await new OpenFoodFactsProvider("https://example.test","NutriCore test").getByBarcode("12345678");expect(item?.nutrients).toMatchObject({energyKcal:42,protein:null});expect(item?.provenance.provider).toBe("OPEN_FOOD_FACTS")});it("does not send invalid barcodes",async()=>expect(new OpenFoodFactsProvider().getByBarcode("oops")).resolves.toBeNull())});
