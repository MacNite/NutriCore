CREATE EXTENSION IF NOT EXISTS pg_trgm;
-- The canonical migration is generated from prisma/schema.prisma during development.
-- Production deploys use Prisma's migration engine; this baseline marker enables controlled upgrades.
